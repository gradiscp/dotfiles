# Run the SDDM greeter's Hyprland with our config instead of Omarchy's
# (/etc/sddm.conf.d is read alphabetically, so this overrides 10-wayland.conf).
# See config/sddm/hyprland.lua for why.
[Wayland]
CompositorCommand=start-hyprland -- --config /usr/local/share/sddm/hyprland.lua
