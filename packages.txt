# Packages beyond a stock Omarchy install. Read by install.sh: one package per
# line, "#" comments and blank lines are ignored. AUR packages go in the
# separate [aur] section and are installed with yay.

# Official repos (pacman)
firefox
tailscale
# Proxmox automation (2026-09-13); installed by hand, listed so a fresh
# install has them too.
ansible
python-proxmoxer

[aur]
bibata-cursor-theme-bin

# NOTE: fingerprint (libfprint/fprintd/usbutils) was tried and reverted -
# this laptop's EgisTec EH57E sensor has no Linux driver, not worth installing.
# What is REMOVED from the stock Omarchy set lives in remove-unwanted-apps.sh.
